<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.WIT.location.mapper.AccommodationMapper">

    <insert id="insertAccommodation" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO accommodations (schedule_id, name, address, phone, latitude, longitude)
        VALUES (#{scheduleId}, #{name}, #{address}, #{phone}, #{latitude}, #{longitude})
    </insert>

    <select id="findAccommodationsByScheduleId" resultType="com.example.WIT.location.dto.AccommodationDto">
        SELECT id, name, address, phone, latitude, longitude
        FROM accommodations
        WHERE schedule_id = #{scheduleId}
    </select>

    <delete id="deleteAccommodationsByScheduleId">
        DELETE FROM accommodations
        WHERE schedule_id = #{scheduleId}
    </delete>

</mapper>
